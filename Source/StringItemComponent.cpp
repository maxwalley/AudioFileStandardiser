/*
  ==============================================================================

    StringItemComponent.cpp
    Created: 12 Dec 2020 10:16:07pm
    Author:  Max Walley

  ==============================================================================
*/

#include "StringItemComponent.h"

/*ComponentListComponent::Item::Item(const juce::String& itemText, int itemId)  : text(itemText), itId(itemId)
{
    addAndMakeVisible(selectedButton);
}

void ComponentListComponent::Item::paint(Graphics& g)
{
    g.drawText(text, 0, 0, getWidth() / 4 * 3, getHeight(), Justification::centred);
}

void ComponentListComponent::Item::resized()
{
    int quarterWidth = getWidth() / 4;
    
    selectedButton.setBounds(quarterWidth * 3 + (quarterWidth - 10), getHeight() / 2 - 10, 20, 20);
}*/
